diff --git a/examples/companion_radio/MyMesh.h b/examples/companion_radio/MyMesh.h
index 1fcc569..8a2fd80 100644
--- a/examples/companion_radio/MyMesh.h
+++ b/examples/companion_radio/MyMesh.h
@@ -200,6 +201,10 @@ private:
 
   TransportKey send_scope;
 
+  // TCP relay helpers (packets from radio to TCP clients via SerialWifiInterface)
+  bool _tcp_relay_enabled;
+  void relayPacketToTcp(const mesh::Packet *pkt);
+
   uint8_t cmd_frame[MAX_FRAME_SIZE + 1];
   uint8_t out_frame[MAX_FRAME_SIZE + 1];
   CayenneLPP telemetry;
